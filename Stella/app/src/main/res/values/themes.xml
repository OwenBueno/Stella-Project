<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Stella" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/stella_background</item>
        <item name="android:colorBackground">@color/stella_background</item>
        <item name="android:textColorPrimary">@color/stella_text_primary</item>
        <item name="android:textColorSecondary">@color/stella_text_secondary</item>
        <item name="android:textColor">@color/stella_text_primary</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>
</resources>
